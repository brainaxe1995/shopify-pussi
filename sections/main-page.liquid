<section class="pk-section">
  <h1 style="font-size:32px;font-weight:900;color:var(--pk-text);margin:0 0 16px;">{{ page.title }}</h1>
  <div style="color:var(--pk-text);line-height:1.7;">{{ page.content }}</div>
</section>
{% schema %}{ "name": "Page", "settings": [] }{% endschema %}
