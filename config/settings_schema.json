[
  {
    "name": "theme_info",
    "theme_name": "Pussikauppa Clone",
    "theme_version": "1.0.0",
    "theme_author": "Internal",
    "theme_documentation_url": "https://shopify.dev",
    "theme_support_url": "https://shopify.dev"
  },
  {
    "name": "Branding",
    "settings": [
      { "type": "image_picker", "id": "favicon", "label": "Favicon" }
    ]
  },
  {
    "name": "Side cart — Flash sale",
    "settings": [
      { "type": "checkbox", "id": "flash_enabled",       "label": "Enable flash sale upsells", "default": true },
      { "type": "range",    "id": "flash_timer_min",     "label": "Timer (minutes)", "min": 1, "max": 120, "step": 1, "default": 15 },
      { "type": "range",    "id": "flash_discount_pct",  "label": "Discount %",      "min": 1, "max": 90,  "step": 1, "default": 25 },
      { "type": "text",     "id": "flash_collection_handle", "label": "Flash source collection (handle)", "info": "Handle of a collection whose products feed the drawer flash section. e.g. 'flash-sale'" }
    ]
  },
  {
    "name": "Shipping bar",
    "settings": [
      { "type": "number", "id": "free_ship_threshold", "label": "Free shipping threshold (in store currency, whole units)", "default": 100 }
    ]
  }
]
